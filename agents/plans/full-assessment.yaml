id: full-assessment
name: Full security assessment
description: Manual investigation triggers all worker contours

routing:
  rules:
    - event_types: [manual.investigation]
      personas: [redteam, network, soc, compliance]
      notify_control: true

hitl:
  auto_approve_dev: true
  trust_threshold: 0.5
