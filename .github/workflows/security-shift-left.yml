name: Security Shift Left

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  security-events: write
  pull-requests: read

jobs:
  secrets:
    uses: ./.github/workflows/secret-scan-reusable.yml
