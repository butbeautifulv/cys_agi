name: Security Shift Left

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  security-events: write
  pull-requests: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "security-shift-left workflow valid"
